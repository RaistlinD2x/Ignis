version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 18
    commands:
      - npm install -g aws-cdk
      - npm install
  build:
    commands:
      - cdk synth
  post_build:
    commands:
      - cdk deploy --context stage=$STAGE_NAME --require-approval never
